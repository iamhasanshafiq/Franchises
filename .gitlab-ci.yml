image: node:18

pages:
  stage: deploy

  script:
    - npm install -g pnpm
    - pnpm install
    - pnpm run build
    - rm -rf public
    - cp -r dist public
    - cp public/index.html public/404.html

  artifacts:
    paths:
      - public

  only:
    - main